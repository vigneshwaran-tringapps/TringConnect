<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@+id/homeIcon"
        android:icon="@drawable/icon_home"
        tools:ignore="MenuTitle" />

    <item android:id="@+id/calenderIcon"
        android:icon="@drawable/icon_calendar"
        tools:ignore="MenuTitle"
       />
    <item android:id="@+id/createIcon"
        android:icon="@drawable/icon_create_post"
        tools:ignore="MenuTitle"
        />
    <item android:id="@+id/folderIcon"
        android:icon="@drawable/icon_folder_view"
        tools:ignore="MenuTitle"
        />
    <item android:id="@+id/chatIcon"
        android:icon="@drawable/icon_chat"
        tools:ignore="MenuTitle"
        />
</menu>